{
  "name": "@agentensemble/e2e",
  "version": "0.1.0",
  "description": "Playwright end-to-end tests for the AgentEnsemble live dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.4.5"
  }
}
