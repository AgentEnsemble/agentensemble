plugins {
    id("agentensemble.tool-conventions")
}

mavenPublishing {
    pom {
        name = "AgentEnsemble Tools: Calculator"
        description = "Arithmetic expression evaluator tool for AgentEnsemble"
    }
}
